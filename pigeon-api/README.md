# Nodejs

npm i express express-session express-rate-limit cocokie-parser morgan dotenv nunjucks passport passport-local passport-kakao sequelize bcrypt mysql2 uuid jsonwebtoken cors
npm i -D nodemon


# 메모
# 다양한 환경의 비밀 키를 발급
# http-proxy-middleware 이용
# 팔로워나 팔로잉 목록을 가져오는 API 만들기(pigeon-api에 새로운 라우터 추가)
# 무료 도메인과 프리미엄 도메인 간에 사용량을 제한을 다르게 적용하기(apiLimiter를 두 개 만들어서 도메인별로 다르게 적용)
# 클라이언트용 비밀 키와 서버용 비밀 키를 구분해서 발급하기
# 클라이언트를 위해 API 문서 작성하기(swagger나 apidoc 사용)